# String Compare

Compares two strings to each other, failing at the first rune difference.

## Testing

Use the included test shell script via `sh test.sh` to run tests. Alternatively,
use `odin test tests/` if you prefer.